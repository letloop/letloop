(sequence

  (define = (xeno =))
  (define - (xeno -))
  
  (define odd
    (lambda (x)
      (if (= x 0)
          #f
          (even (- x 1)))))

  (define even
    (lambda (x)
      (if (= x 0)
          #t
          (odd (- x 1)))))


  (pk (odd 12))
  (pk (even 12)))


           
    
