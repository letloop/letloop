(define string-upper (xeno string-upcase))
(define string-lower (xeno string-downcase))
(define string->list (xeno string->list))
(define list->string (xeno list->string))
(define reverse (xeno reverse))

(define display (xeno display))
(define newline (xeno newline))
(define write (xeno write))

(define print
  (lambda (message)
    (display message)
    (newline)))

(define print*
  (vau (exp) env
    (display "> ") (write exp)
    (newline)
    (print (eval exp env))))

(print* (string-upper "ⴰⵣⵓⵍ ⴼⴻⵍⴰⵡⴻⵏ"))
(newline)
(print* (string-upper "Approchez, et soyez aussi bon que mon coeur."))
(newline)
(print* (string-upper "اقترب وكن لطيفا مثل قلبي"))
(newline)
(print* (string-upper "Come to me."))
